import { Sidebar } from '../../components/';
import { AppLayout, MainLayout } from "../../layouts";

export const Boards = () => {
  return (
    <AppLayout>
      <Sidebar />
      <MainLayout title="My Boards">
        ""
      </MainLayout>
    </AppLayout>
  );
};
